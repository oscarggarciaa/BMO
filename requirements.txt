# --- Core / arquitectura ---
PyYAML>=6.0            # carga de config.yaml

# --- Visión (Fase 1) ---
numpy>=1.24
opencv-python>=4.8     # detección Haar (CPU). Reemplazable por AI Camera IMX500 a futuro.
# picamera2           # SOLO en la Raspberry Pi (viene con Raspberry Pi OS, no via pip en Windows)

# --- Cerebro (Fase 3) ---
ollama>=0.3.0          # cliente del LLM local con tool calling

# --- Voz / TTS ---
# piper-tts           # SOLO en la Pi: TTS neuronal offline (provee el binario `piper`)
#   pip install piper-tts   y descargar un modelo .onnx (+ .onnx.json), p. ej. en_US-lessac-medium
#   La reproduccion usa `aplay` (viene con alsa-utils: sudo apt install alsa-utils)

# --- Oido / STT (wake-word 'hello') ---
# vosk                # SOLO en la Pi: STT neuronal offline (reconocimiento de voz)
#   pip install vosk
#   Descargar el modelo small en-us (~40MB) y descomprimir en models/:
#     cd models
#     wget https://alphacephei.com/vosk/models/vosk-model-small-en-us-0.15.zip
#     unzip vosk-model-small-en-us-0.15.zip
#   La captura usa `arecord` (viene con alsa-utils)

# --- Cara en pantalla (Fase 6) ---
Pillow>=10.0           # carga y escalado de los frames PNG para tkinter
# En la Raspberry (venv con --system-site-packages) ImageTk NO viene con pip:
#   sudo apt install python3-pil.imagetk

# --- Tests ---
pytest>=8.0
